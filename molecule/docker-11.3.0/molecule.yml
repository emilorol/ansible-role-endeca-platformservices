---
dependency:
  name: galaxy
  options:
    role-file: requirements.yml
driver:
  name: docker
lint:
  name: yamllint
  options:
    config-file: .yamllint
platforms:
  - name: test-docker-centos6-endeca-platformservices
    image: centos:6
    privileged: True
    security_opts:
      - seccomp=unconfined
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
    tmpfs:
      - /tmp
      - /run
    capabilities:
      - SYS_ADMIN
    groups:
      - ansible_role_docker_ct
provisioner:
  name: ansible
  lint:
    name: ansible-lint
scenario:
  name: docker-11.3.0
verifier:
  name: testinfra
  lint:
    name: flake8
